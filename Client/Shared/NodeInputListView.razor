@using VirtualStudio.ViewModels
@using System.Collections.ObjectModel;

@foreach (var input in ViewModel)
{
    <NodeEndpointDescriptionView ViewModel="input"></NodeEndpointDescriptionView>
}

@code {
    [Parameter] public ObservableCollection<StudioComponentEndpointViewModel> ViewModel { get; set; }
}
